# PRD: 학교 지원 및 결제 프로세스

## 1. 개요

### 1.1 목적
학생이 원하는 대학교를 선택하고, 지원서를 작성한 후 결제까지 완료하는 전환 퍼널 구축

### 1.2 핵심 지표 (KPI)
- 지원서 시작률 (Apply Now 클릭)
- 지원서 완료률
- 결제 전환율
- 평균 주문 금액

---

## 2. 사용자 플로우

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  랜딩페이지  │ ──▶ │  학교 상세   │ ──▶ │  지원서 작성  │ ──▶ │   결제 페이지  │ ──▶ │  완료 페이지  │
│             │     │             │     │             │     │             │     │             │
│ 학교 목록    │     │ Apply Now   │     │ Step 1-3    │     │ 결제 정보    │     │ 확인 & 영수증 │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
```

### 2.1 상세 플로우

| 단계 | 페이지 | URL | 설명 |
|------|--------|-----|------|
| 1 | 학교 상세 | `/schools/[id]` | Apply Now 버튼 클릭 |
| 2 | 지원서 작성 | `/apply/[schoolId]` | 3단계 폼 |
| 3 | 결제 | `/apply/[schoolId]/payment` | 결제 정보 입력 |
| 4 | 완료 | `/apply/[schoolId]/complete` | 확인 및 다음 단계 안내 |

---

## 3. 페이지 설계

### 3.1 지원서 작성 페이지 (`/apply/[schoolId]`)

#### Step 1: 기본 정보
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| firstName | text | ✅ | 이름 (영문) |
| lastName | text | ✅ | 성 (영문) |
| email | email | ✅ | 이메일 |
| phone | tel | ✅ | 전화번호 |
| nationality | select | ✅ | 국적 |
| dateOfBirth | date | ✅ | 생년월일 |
| gender | select | ✅ | 성별 |

#### Step 2: 학력 정보
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| educationLevel | select | ✅ | 최종학력 |
| schoolName | text | ✅ | 학교명 |
| major | text | ❌ | 전공 |
| graduationYear | select | ✅ | 졸업연도 |
| gpa | text | ❌ | 학점/GPA |
| languageTest | select | ❌ | 어학시험 (TOPIK/IELTS/TOEFL) |
| languageScore | text | ❌ | 점수 |

#### Step 3: 지원 정보
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| intendedProgram | select | ✅ | 지원 프로그램 |
| intendedSemester | select | ✅ | 입학 학기 |
| motivation | textarea | ✅ | 지원 동기 (500자 이상) |
| documents | file[] | ❌ | 추가 서류 업로드 |

#### UI 구성
```
┌────────────────────────────────────────────────────────────┐
│  [학교 로고/이미지]  Seoul National University              │
│  ─────────────────────────────────────────────────────────  │
│                                                            │
│  Step 1 ● ─── Step 2 ○ ─── Step 3 ○                        │
│  기본 정보      학력 정보      지원 정보                      │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  이름 (영문) *                                        │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │                                                │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │                                                      │  │
│  │  성 (영문) *                                          │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │                                                │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │                                                      │  │
│  │  ...                                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                    [다음 단계 →]     │   │
│  └─────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

---

### 3.2 결제 페이지 (`/apply/[schoolId]/payment`)

#### 결제 항목
| 항목 | 금액 | 설명 |
|------|------|------|
| 지원 수수료 | $50 | 기본 서비스 비용 |
| 서류 대행 | $100 | 서류 번역/공증 대행 |
| 프리미엄 컨설팅 | $200 | 1:1 입학 컨설팅 (선택) |

#### 결제 수단
- 신용카드/체크카드 (Stripe)
- PayPal
- 계좌이체 (한국 사용자)

#### UI 구성
```
┌────────────────────────────────────────────────────────────┐
│  결제 정보                                                  │
│  ─────────────────────────────────────────────────────────  │
│                                                            │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  │
│  │  주문 요약               │  │  결제 수단              │  │
│  │  ───────────────────    │  │  ───────────────────    │  │
│  │  Seoul National Univ.   │  │  ○ 신용카드            │  │
│  │                         │  │  ○ PayPal              │  │
│  │  지원 수수료    $50     │  │  ○ 계좌이체            │  │
│  │  서류 대행      $100    │  │                         │  │
│  │  ─────────────────────  │  │  카드 번호              │  │
│  │  총 금액       $150     │  │  ┌─────────────────┐    │  │
│  │                         │  │  │                 │    │  │
│  │  [프로모션 코드 입력]    │  │  └─────────────────┘    │  │
│  │                         │  │                         │  │
│  └─────────────────────────┘  │  만료일 / CVC           │  │
│                               │  ┌───────┐ ┌───────┐    │  │
│                               │  │       │ │       │    │  │
│                               │  └───────┘ └───────┘    │  │
│                               │                         │  │
│                               │  [💳 $150 결제하기]      │  │
│                               └─────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

---

### 3.3 완료 페이지 (`/apply/[schoolId]/complete`)

#### 표시 정보
- 지원 완료 확인 메시지
- 지원 번호 (Reference Number)
- 결제 영수증 다운로드
- 다음 단계 안내
- 이메일 발송 확인

#### UI 구성
```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│                      ✅                                    │
│              지원이 완료되었습니다!                          │
│                                                            │
│  ─────────────────────────────────────────────────────────  │
│                                                            │
│  지원 번호: APP-2026-001234                                │
│  학교: Seoul National University                           │
│  프로그램: Computer Science                                │
│  결제 금액: $150                                           │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  📧 확인 이메일이 발송되었습니다                        │  │
│  │  (student@email.com)                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
│  📋 다음 단계                                              │
│  1. 이메일에서 서류 제출 안내를 확인하세요                   │
│  2. 7일 이내에 추가 서류를 업로드하세요                      │
│  3. 담당자가 검토 후 연락드립니다                           │
│                                                            │
│  ┌────────────────┐  ┌────────────────┐                   │
│  │ 📄 영수증 다운로드 │  │ 🏠 홈으로 돌아가기 │                   │
│  └────────────────┘  └────────────────┘                   │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 4. 데이터 모델

### 4.1 Application (지원서)
```typescript
interface Application {
  id: string;

  // 관계
  userId: string;
  schoolId: string;

  // 기본 정보
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  nationality: string;
  dateOfBirth: string;
  gender: 'male' | 'female' | 'other';

  // 학력 정보
  educationLevel: 'high_school' | 'bachelor' | 'master' | 'phd';
  schoolName: string;
  major?: string;
  graduationYear: number;
  gpa?: string;
  languageTest?: 'topik' | 'ielts' | 'toefl' | 'none';
  languageScore?: string;

  // 지원 정보
  intendedProgram: string;
  intendedSemester: string;
  motivation: string;
  documents?: string[];

  // 상태
  status: 'draft' | 'submitted' | 'paid' | 'under_review' | 'accepted' | 'rejected';

  // 결제 정보
  paymentId?: string;
  paymentStatus?: 'pending' | 'completed' | 'failed' | 'refunded';
  paymentAmount?: number;

  // 타임스탬프
  createdAt: Timestamp;
  updatedAt: Timestamp;
  submittedAt?: Timestamp;
  paidAt?: Timestamp;
}
```

### 4.2 Payment (결제)
```typescript
interface Payment {
  id: string;
  applicationId: string;
  userId: string;

  // 결제 정보
  amount: number;
  currency: 'USD' | 'KRW';
  method: 'card' | 'paypal' | 'bank_transfer';

  // Stripe 정보
  stripePaymentIntentId?: string;
  stripeCustomerId?: string;

  // 상태
  status: 'pending' | 'processing' | 'completed' | 'failed' | 'refunded';

  // 프로모션
  promoCode?: string;
  discountAmount?: number;

  // 영수증
  receiptUrl?: string;

  // 타임스탬프
  createdAt: Timestamp;
  completedAt?: Timestamp;
}
```

---

## 5. API 엔드포인트

### 5.1 지원서 API
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/applications` | 지원서 생성 |
| GET | `/api/applications/[id]` | 지원서 조회 |
| PUT | `/api/applications/[id]` | 지원서 수정 |
| POST | `/api/applications/[id]/submit` | 지원서 제출 |

### 5.2 결제 API
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/payments/create-intent` | Stripe PaymentIntent 생성 |
| POST | `/api/payments/confirm` | 결제 확인 |
| GET | `/api/payments/[id]` | 결제 정보 조회 |
| POST | `/api/payments/[id]/refund` | 환불 요청 |

---

## 6. 기술 스택

### 6.1 프론트엔드
- Next.js 16 (App Router)
- React Hook Form (폼 관리)
- Zod (유효성 검사)
- Stripe Elements (결제 UI)

### 6.2 백엔드
- Firebase Firestore (데이터베이스)
- Firebase Functions (서버리스)
- Stripe API (결제 처리)

### 6.3 이메일
- SendGrid / Resend (트랜잭션 이메일)

---

## 7. 보안 고려사항

### 7.1 데이터 보호
- 개인정보 암호화 저장
- HTTPS 필수
- PCI DSS 준수 (Stripe 위임)

### 7.2 인증
- 로그인 필수 (지원서 작성 시)
- 본인 지원서만 접근 가능
- Admin만 모든 지원서 열람

---

## 8. 구현 우선순위

### Phase 1 (MVP)
- [ ] 지원서 작성 페이지 (3단계 폼)
- [ ] 지원서 Firestore 저장
- [ ] 결제 페이지 (Stripe 연동)
- [ ] 완료 페이지

### Phase 2
- [ ] 이메일 발송 (확인/알림)
- [ ] 프로모션 코드 시스템
- [ ] 지원 현황 대시보드

### Phase 3
- [ ] 서류 업로드 기능
- [ ] Admin 지원서 관리
- [ ] 환불 프로세스

---

## 9. 파일 구조

```
src/
├── app/
│   └── apply/
│       └── [schoolId]/
│           ├── page.tsx           # 지원서 작성
│           ├── payment/
│           │   └── page.tsx       # 결제
│           └── complete/
│               └── page.tsx       # 완료
├── components/
│   └── apply/
│       ├── ApplicationForm.tsx    # 지원서 폼
│       ├── StepIndicator.tsx      # 단계 표시
│       ├── PersonalInfoStep.tsx   # Step 1
│       ├── EducationStep.tsx      # Step 2
│       ├── ProgramStep.tsx        # Step 3
│       ├── PaymentForm.tsx        # 결제 폼
│       └── OrderSummary.tsx       # 주문 요약
├── lib/
│   └── services/
│       ├── applicationService.ts  # 지원서 CRUD
│       └── paymentService.ts      # 결제 처리
└── types/
    └── application.ts             # 타입 정의
```

---

## 10. 예상 일정

| 항목 | 설명 |
|------|------|
| 데이터 모델 & 타입 정의 | Application, Payment 타입 |
| 지원서 폼 UI | 3단계 폼 컴포넌트 |
| Firestore 연동 | 지원서 저장/조회 |
| Stripe 결제 연동 | PaymentIntent, Elements |
| 완료 페이지 | 확인 & 영수증 |
| 테스트 | E2E 테스트 |

---

*문서 작성일: 2026-02-09*
*버전: 1.0*
